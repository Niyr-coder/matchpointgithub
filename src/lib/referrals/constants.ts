/** Cookie httpOnly-safe name (set from client; read on server al completar onboarding). */
export const MP_REF_COOKIE = "mp_ref";

/** 30 días — ventana razonable entre click en link y registro. */
export const MP_REF_COOKIE_MAX_AGE_SEC = 60 * 60 * 24 * 30;
