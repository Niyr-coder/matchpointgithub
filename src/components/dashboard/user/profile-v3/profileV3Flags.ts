/** Showcase con pins reales (localStorage / modal). Off hasta cablear datos como ProfileScreenView. */
export const PROFILE_V3_SHOWCASE_ENABLED = false;

/** Scout MP+ al visitar otro perfil (requiere `visitorRetarContext` con payload scout). */
export const PROFILE_V3_SCOUT_ENABLED = true;
